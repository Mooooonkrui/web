{
  "name": "site-ecoli",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node ../../scripts/build-site.mjs src dist",
    "dev": "npx serve src",
    "deploy": "wrangler pages deploy dist --project-name=site-ecoli",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
