when:
  - event: push
    branch: main

steps:
  build:
    image: node:20
    commands:
      - corepack enable
      - corepack prepare pnpm@latest --activate
      - pnpm install --frozen-lockfile
      - pnpm turbo build
